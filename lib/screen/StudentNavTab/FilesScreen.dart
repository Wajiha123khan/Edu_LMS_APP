import 'package:flutter/material.dart';

class Filesscreen extends StatelessWidget {
  const Filesscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Hi File Screen")));
  }
}
